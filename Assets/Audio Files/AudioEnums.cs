namespace JSAM {
    public enum Sounds {
        back,
        buttonhover,
        buttonselect,
        confirm,
        gamestart,
        playerjoin,
        playerleave
    }
    public enum Music {
    }
}
